<h1>ユーザー一覧</h1>
<div class="profile_list">
  <table>
    <theader>
      <tr>
        <th>名前</th><th>性別</th><th>生年月日</th><th>出身</th>
      </tr>
    </theader>
  <tbody>
    <% @users.each do |user| %>
        <tr>
          <td>
            <%= link_to user.name, user_path(user) %>
          </td>
          <td>
            <%= user.gender %>
          </td>

          <td>
            <%= user.birthday %>
          </td>

          <td>
            <%= user.hometown %>
          </td>
        </tr>

    <% end %>

    </tbody>
  </table>
</div>
